# 런타임 의존성 — 단일 화면 요약 프로토타입. 실제로 import 하는 것만 둔다.
streamlit>=1.40

# --- 기능 추가 시 주석 해제 ---
# pandas>=2.0       # 실행 이력 드릴다운 / 표 가공
# plotly>=5.20      # 타임라인 · 차트
# boto3>=1.34       # AWS Glue (실제 연결)
# requests>=2.31    # Airflow REST API / Airbyte API (실제 연결)
