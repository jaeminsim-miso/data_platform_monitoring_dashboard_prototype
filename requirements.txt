# 런타임 의존성 — 단일 화면 요약 프로토타입. 실제로 import 하는 것만 둔다.
streamlit>=1.40
boto3>=1.34            # AWS Glue Job Run 실데이터 (boto3 Glue API)
psycopg2-binary>=2.9  # Airbyte 메타DB (인클러스터 Postgres)
requests>=2.31        # Airflow REST API (basic_auth)

# --- 기능 추가 시 주석 해제 ---
# pandas>=2.0       # 실행 이력 드릴다운 / 표 가공
# plotly>=5.20      # 타임라인 · 차트
